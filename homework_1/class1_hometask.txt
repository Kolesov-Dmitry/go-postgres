-- create table
CREATE TABLE IF NOT EXISTS users (
    id         BIGSERIAL NOT NULL,
    first_name VARCHAR(50) NOT NULL,
    last_name  VARCHAR(50) NOT NULL,
    email      VARCHAR(50) NOT NULL,
    pwd        VARCHAR(72) NOT NULL,
    created_at TIMESTAMP NOT NULL DEFAULT 'now()',
    PRIMARY KEY (id)
);

-- insert test users
INSERT INTO users (first_name, last_name, email, pwd) VALUES
    ('first',  'user', 'first@email.org',  '123'),
    ('second', 'user', 'second@email.org', '456'),
    ('third',  'user', 'third@email.org',  '789');


У меня так и не удалось заставить работать команду \d в psql.
Я пробовал и назначать пользователю все права на базу, и создавал 
разные схемы, кроме public, он всё равно выводит сообщение "relations not found"...